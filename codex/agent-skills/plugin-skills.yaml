# Codex CLI plugin skills
sourcePath: ~/.codex/plugins/cache/*/*/{latest}/**/SKILL.md
name: "{frontmatter@name}"
label: "{pathdir:1} / {pathdir:2}"
description: "{frontmatter@description}"
argumentHint: "{frontmatter@argument-hint}"
triggers: ["$"]
