# Claude Code agents (from ~/.claude/agents/*.md, search with @agent:)
pluginDescription: "~/.claude/agents/*.md, search with @agent:"
name: "{basename}(agent)"
label: user
description: "{frontmatter@description}"
path: ~/.claude/agents
pattern: "*.md"
searchPrefix: agent
displayTime: "{updatedAt}"
