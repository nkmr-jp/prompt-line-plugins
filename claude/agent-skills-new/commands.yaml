# Claude Code global commands
# See: https://code.claude.com/docs/en/claude-directory#ce-global-commands
name: "{basename}"
label: global
sourcePath: ~/.claude/commands/*.md
description: "{frontmatter@description}"
argumentHint: "{frontmatter@argument-hint}"
